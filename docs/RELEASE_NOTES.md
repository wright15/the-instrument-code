# Release Notes

## Integrated release 1.3.0 - 2026-08-10

- Admits the bounded EPIC-002 Governor runtime and EPIC-003 Pentatonic Court
  through separate machine-readable admission evidence without modifying frozen
  package internals.
- Adds GOV-208 deterministic read-only Obsidian context bundles and CRT-308
  Court extensions with exact no-provider parity, bounded traversal, strict
  frontmatter, privacy controls, and false-admission downgrading.
- Adds CRT-309's 18-criterion one-command validator, ten-case machine-scored
  benchmark, native live Neo4j parity, explicit narrow scope, and exact
  artifact/evidence bindings.
- Preserves 35 pentatonic classes, aggregate `C_H`, non-linear/non-diagonal
  filters, natural-phenomena, thermodynamic mappings, private vault content,
  live sessions, and model prose outside admitted canonical authority.

## Court agent skill bundle 1.0.0 - 2026-08-10

- Adds five strict Court workflows through a parallel CRT-307 facade without
  changing GOV-207 operation IDs, schemas, workflows, adapters, or install
  paths.
- Replays CRT-305 state before every operation and keeps validation tokens,
  trusted translocation records, filter selection, and verifier decisions out
  of model-facing requests.
- Requires exact menu/state/capability closure, typed verification, semantic
  replay, postcondition closure, and external CAS persistence before reporting
  a verified Court transition.
- Adds read-only current-position `P_c` projection, historical event
  verification without rerunning effects, optional bounded CRT-306 graph
  corroboration, and deterministic replan/stop handling.
- Publishes strict schemas, Hermes/generic adapters, a non-destructive
  explicit-target installer, eight evaluation traces, and an observational
  8/8 pass against a local Qwen 35B endpoint. Model output is excluded from
  canonical fingerprints; admission remains `proposed_pending_crt_309`.

## Court graph projection compatibility v2 - 2026-08-10

- Amends the completed CRT-306 read projection to consume independently
  replayed CRT-305 genesis/events/anchor input instead of caller-authored
  legacy Court runtime state.
- Adds runtime session, transition event, ledger snapshot, terminal state, and
  topological translocation records without making Neo4j an authorization or
  replay authority.
- Binds the verified C1 to C4 R7/5-23 fixture event to the exact immutable
  CRT-304 route row; static commutation ledger pointers remain null.
- Publishes two bounded session/event queries for a six-query catalog and a
  deterministic 21-node, 19-relationship, one-ScaleState-reference fixture.
- Adds fail-closed serialized evidence checks, exact route metadata closure,
  negative live-Neo4j validation, reset/rebuild parity, and hash-seed/time-zone
  determinism. Admission remains `proposed_pending_crt_309`.

## Court runtime policy 0.1.0 - 2026-08-09

- Adds the CRT-305 root-owned candidate policy and verified runtime without
  changing the GOV-204 `AgentState` or generic `LedgerEvent` contracts.
- Derives all five masks, pole registers, and normalized exact
  `kappa_court = i/4` values; rejects seven cross-namespace write targets.
- Enumerates the eight directed adjacent moves and binds single-use validation
  tokens to state, ledger, policy, context, capability, operation, expiry, and
  optional translocation/route evidence.
- Requires typed passing GOV-205 verification decisions before committing a
  Court event; stale, reused, expired, or mismatched tokens produce no delta.
- Adds forward/reverse R7/L7 compound translocation evidence for both 5-23 and
  5-27 routes without inventing a canonical Forte-to-Court mapping.
- Adds strict state/move/token/event/translocation/snapshot/replay/session
  schemas, semantic replay and tamper detection, and an external locked atomic
  CAS session store under `XDG_STATE_HOME` by default.

## Court filter algebra 0.1.0 - 2026-08-09

- Adds the CRT-304 versioned candidate package over CRT-302, CRT-303, and the
  admitted mutation audit without changing integrated release 1.2.0.
- Publishes seven strict `linear_diagonal` operators with explicit ambient
  domain, image, inverse, exact bit reduction, preservation, and five-valued
  commutation declarations.
- Independently reproduces all 3,402 mutation applications and evaluates seven
  filters against 15 operators and 462 operands, totaling 48,510 cases.
- Emits 23,814 typed mutation-first route asymmetries and records 24,696
  both-undefined cases; no two-defined equality is fabricated outside the
  admitted mutation domain.
- Proves rooted 5-23 and 5-27 expose distinct information on the Aeolian to
  Harmonic Minor bridge, while route cost remains unresolved and spectral
  measures remain not admitted.
- Corrects the older C2/L7 graph fixture and removes invented ledger pointers;
  concrete route-event binding remains a CRT-305 obligation.

## Harmonic invariant registry 0.1.0 - 2026-08-09

- Adds the CRT-303 versioned candidate package over the fingerprinted CRT-302
  substrate without changing integrated release 1.2.0.
- Computes `G_Court = 2I_4`, the complete Hamming path matrix, disjoint XOR
  supports, weight-five values, and exact `kappa_court = i/4` ratios.
- Replaces premise-supplied Carey counts with an independent 5-35 enumerator:
  20 interval instances, `D=20`, zero ambiguities or contradictions, `F=0`,
  `CQ=1`, and `SQ=1/2`.
- Rejects non-5-35 scoped Carey requests and preserves lower-level results as
  diagnostic only.
- Adds strict provenance, negative fixtures, deterministic canonical builds,
  root validator integration, and an unresolved aggregate `C_H` namespace
  guard.

## Court substrate registry 0.1.0 - 2026-08-09

- Adds a new versioned candidate package with all 38 pentatonic set classes,
  strict complement maps, and explicit admission blockers for the 35 classes
  outside EPIC-003 scope.
- Materializes C0-C4 with exact masks, rational `kappa_court`, pole registers,
  XOR supports, and selected T5 offsets.
- Proves rooted 5-23 and 5-27 independently mediate Aeolian to Harmonic Minor;
  no additional bridge class is minimally required.
- Distinguishes the full 12-entry T5 root cycle from its five-entry Court
  segment and raw complement masks from normalized root-0 state pointers.
- Corrects two mislabeled bridge masks in the mathematical realization draft.
- Passes strict schemas, positive/negative fixtures, source hashing, reordered
  inputs, and two-clean-build determinism while remaining pending CRT-309.

## Court admission authority contract 1.0.0 - 2026-08-09

- Accepts CRT-301's root-owned Court namespace and authority contract without
  admitting the Court subsystem or modifying frozen versioned packages.
- Supersedes the pending "all 38 pentatonic set classes" scope with C0-C4 of
  5-35, 5-23, 5-27, and only bridge classes proven minimally necessary by
  CRT-302.
- Adds strict exact-ratio `kappa_court` values and the machine-enforced guard
  separating it from `C_P`, `C_H`, `C_S`, and physical quantities.
- Locks Court operations and projections out of canonical office, Degree
  Governor, primary/operational Governor, and heptatonic-topology writes.
- Keeps natural phenomena, thermodynamic mappings, semantic enrichment, and
  excluded Fivefold/filter material proposed for separate work.

## Governor runtime contracts 0.1.0 - 2026-08-01

- Adds a post-1.2.0 candidate package with strict `TypedAspect`, `Quantity`,
  `BridgeRule`, `ClassificationRequest`, `ClassificationResult`, and policy
  release schemas.
- Crosswalks all 31 FeatureDefinitions exactly once and types four legacy
  compiler compatibility strings outside that feature vocabulary.
- Adds registered dimensional operations and non-causal Jupiter 470 nm,
  Rayleigh, atmospheric/aeolian, and symbolic-profile examples.
- Adds expected-failure fixtures, source/policy fingerprints, separate-process
  check/emit/check, two-clean-build and reordered-input proofs, and root
  validation coverage.
- Keeps integrated admission proposed and leaves topology, Neo4j, frozen
  packages, Court/Fivefold, phenomena admission, and pentatonic scope unchanged.

## 5.0.0 — Topology identity and Neo4j formalization

- Defines the necessary identity properties of A anchors, D anchors,
  satellites, convergence rings, junctions, leaves, and boundary states.
- Distinguishes convergence evidence from D-tier promotion.
- Records relational-office evidence as first-class non-categorical state
  properties and Neo4j relationships.
- Formalizes the audit, canonical-release, Neo4j, and renderer layers.
- Adds 507 Neo4j nodes and 2,818 relationships, including 224 derived
  relational-office-evidence projections.
- Adds constraints, standard-Cypher CSV import, executable invariant queries,
  and example exploration queries.
- Adds offline export validation with 58 passing checks.
- Adds Cypher syntax validation for all five Cypher sources.
- Packages the graph template, builder, validator, and exact interactive graph.

## 4.0.0 — Universal heptatonic completion

## Added

- Complete 462-state rooted heptatonic ledger.
- Forte and modal-orientation registries covering all 38 classes and 66
  orientation orbits.
- Formal 7-1 audit and seven validated D7 terminal anchors.
- Fourteen D7 seat-contact records, including twelve fixed and two phase cases.
- Typed audits for all eleven residual chiral families.
- 476 non-governing boundary relation edges.
- Consolidated A0–D7 network JSON.
- Eleven-sheet audit workbook with formula-backed release gates.
- Independent structural validator and post-export workbook validator.

## Preserved

- Every A0–D6 role, tier, and Governor office.
- Existing fixed-tonic and phase evidence.
- The left-to-right office order:
  Sun, Moon, Mars, Mercury, Jupiter, Venus, Saturn.

## Refined

Previously generic boundary fine roles are classified as:

- `oriented_convergence`;
- `office_junction`;
- `peripheral_leaf`.

This is a non-governing metadata refinement. It does not alter their null office
assignments.

## Decision summary

7-1 is the final unrepresented achiral heptatonic class and closes the current
anchor sequence as D7. The other missing or partially represented families are
not omitted scales; they occupy the chiral relational boundary of the network.
