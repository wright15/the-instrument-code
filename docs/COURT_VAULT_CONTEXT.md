# Optional Court Vault Context

CRT-308 layers Court fields over GOV-208 without changing CRT-305 runtime
policy, CRT-306 query fingerprints, or CRT-307 skill contracts. Court context
is transparent pedagogy and evidence only; runtime replay and CRT-309 admission
remain authoritative.

Court notes add `courtRootedPosition`, `pentatonicSetClass`, `kappaCourt`,
`courtFilterMask`, and `courtProvenanceRef`. The compiler binds C0-C4 masks and
exact kappa values to CRT-302, validates CRT-303 invariant references, and
preserves 5-23/5-27 as `admitted-bridge`. A note that labels another set class
as admitted is deterministically downgraded to `proposed` with CRT-309 named as
the responsible gate.

```markdown
---
noteId: court:c0:gram
admissionStatus: admitted
source: source:operator:vault
sensitivity: public
courtRootedPosition: C0
pentatonicSetClass: 5-35
kappaCourt: 0
courtFilterMask: 101010010100
courtProvenanceRef: crt-303:court.gram_matrix
---
The four canonical Court edges have Gram matrix 2I4.
```

The emitted Court bundle carries selected synthetic/public context, structural
diagnostics, dependency fingerprints, and explicit false flags for canonical
policy, admission, and graph-query changes. No live vault is required for tests
or release validation.
