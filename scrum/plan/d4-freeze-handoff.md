# D4 Frozen Boundary Handoff

**Status:** definition frozen by `provenance/DECISION_LEDGER.md`, ENTRY 9,
2026-09-14. This handoff is ledger-to-Scrum synchronization, not new authority.

The reviewed bytes remain in `scrum/plan/d4-boundary-draft.md` despite its
historical filename and draft-status prose. Their SHA-256 is
SHA-256 `751d56d4f01ea1a0d054c8119d178a64b20787aa9fb095a02c99f3ba3b135de2`.
Do not edit those bytes to update status. ENTRY 9 supersedes process-status
notes only and binds the targets, semantics, comparator and completed contract.

ENTRY 9 includes the four-amendment review, the retained reachability hold,
the approved retrograde orientation correction, the GOVERNS input declaration,
all seven disposition categories and the D4 negative-result qualifier. Its
same-landing attachments contain all five comparator counts, the R-bounded
conformance rule, and the source-only dissociation demonstrations.

## Next Gate

The implementation-spec phase is eligible next, under separate review and
authorization. No engine, scaffolding, implementation or production run is
authorized by the freeze. No D4 verdict exists. T-B's complete R restatement
is disclosed; it cannot silently earn the strict `derived` category.
The recurrence probe remains definition-blocked behind the D4 verdict.

## Implementation-Spec Review Queue

The spec is not authored or approved by this checklist. Relay it through the
maintainer review seat under its separate gate, checking:

1. Call graph and route isolation, including shared-helper and cross-read rules.
2. Faithful implementation of the frozen comparator endpoint directions,
   contact-to-parent joins and seam provenance fields; no new field semantics.
3. Five-field receipt mechanics, sorted witnesses, equality/restatement flags,
   and beyond-R nonconformance precedence.
4. Controls and clean counterparts mapped to all seven Outcome Contract rows,
   with unavailable result shapes disclosed rather than forced into reachability.
5. Verbatim faithfulness of T-A/T-B/T-C predicate pseudocode to the frozen text,
   especially `o(a)=k+1 mod 7`, `o(b)=k-1 mod 7`, `T+1(m(a))=m(b)`, `o(h)=k`.

The promotion-evidence pin migration must be green before or with the first
new D4 ledger edit. It is now verified separately in `ledger-pin-migration.md`;
perform one terminal cascade after the eventual authorized spec/content pass.

## Freshness Scope

The freeze landing runs ledger-dependent sidecar/closure regeneration followed
by UI projection regeneration and packaging/validation. The old 416/2 receipt
does not certify the changed tree. The integrated validator must independently
name the remaining failures; expected open authority gates are frozen composite
payload identity and retained-versus-current Neo4j evidence. They are blocking,
not waived. This is not a release closeout or a green fixed-point claim.

## HALT

Preserve the reviewed boundary and both historical correction records.
Do not emit a D4 outcome or treat source-only proof counts as contact coverage.
No release flip, topology/admission change, commit, tag or push follows.

## Landing Verification

The freeze landing regenerated shadow-ladder, twin-hub, fifth-space census and
pentatonic binding closure, then field-derivation, taxonomy and D-tier UI bundles.
The structural input bytes and frozen boundary hash did not change. The fresh
integrated receipt at `qa/integrated-release-validation.json` reports **416
passed / 2 failed**; its two failures are exactly:

- `frozen composite package payload identities`.
- `declared full-database reproducibility and deployment evidence`.

Both are the previously named blocking authority questions, not silently
tolerated failures or new unnamed regressions. This is a fail-closed landing,
not a green release fixed point. The baseline/pin decisions remain outside
this freeze's grant. Release identity remains `1.9.0-dev`.

| Command Or Suite | Actual Result |
|---|---|
| `npm run build:shadow-ladder --silent` and `npm run validate:shadow-ladder --silent` | Ran; 37/0 checks and 6 tests passed |
| `npm run build:twin-hub --silent` and `npm run validate:twin-hub --silent` | Ran; 30/0 checks and 8 tests passed |
| `npm run build:fifth-space-census --silent` and `npm run validate:fifth-space-census --silent` | Ran; 24/0 checks and 8 tests passed |
| `node scripts/build-pentatonic-binding-audit-closure.mjs` | Ran; 11/0 closure checks passed |
| `npm run orrery:field-derivation:build --silent` and `npm run orrery:field-derivation:check --silent` | Ran; 7/0 checks passed |
| `node scripts/build-orrery-taxonomy-read-model.mjs` then `node scripts/build-orrery-d-tier-taxonomy-dataset.mjs` | Ran; 462 and 175 source-derived records regenerated |
| `npm run orrery:check`, `npm run orrery:test`, `npm run orrery:build` | Ran; checks/types/build passed, 144 unit tests passed; chunk-size warning retained |
| `node --test orrery/tests/taxonomy-builders.node.mjs` | Ran; 3 tests passed |
| `npm run package:manifest --silent` and `node scripts/build-manifest.mjs --check` | Ran; generation and freshness checks passed |
| `npm run validate --silent` with native project Python | Ran; integrated result 416/2 as named above, downstream chained stages short-circuited |
| `npm run validate:fivefold-engine-promotion-evidence --silent` | Ran; `STALE_FIVEFOLD_ENGINE_PROMOTION_EVIDENCE` before validator/tests; not a pass |

The separate legacy promotion check retains hard-coded ledger pin `7a3d3236...`
in both builder and validator, predating ENTRY 9. Its class fix is already queued
in the fingerprint map; this landing does not silently rewrite an admission
evidence pin or promote that failure into a green receipt. It is outside the
integrated 418-check count and remains an explicitly reported residual gap.

Subsequent class-fix update: the preceding paragraph describes the freeze-time
failure, not current status. `ledger-pin-migration.md` records removal of those
two constants, artifact-carried freshness, successful regeneration/validation,
and the justified-allowlist inventory guard. Historical admission evidence pins
and the immutable D4 boundary are not rewritten by that migration.

The first standalone prose scan failed solely on the handoff's unlabelled hash
line; that line was corrected without changing the bound draft. Prose and Cypher
must be run separately because the integrated command stops at its red receipt.
Logs are local execution output under `/tmp/opencode/d4-freeze-*.log`.

**Skipped:** D4 engine/control pre-flight and contact/seam comparison, because
the freeze grants no execution. **Skipped:** fresh browser and full Neo4j runs,
because this is a definition/freshness landing, not product release closure;
historical browser receipt hashes refer to pre-cascade UI bundles and are not
fresh browser evidence for the regenerated bytes. No baseline was recaptured.
