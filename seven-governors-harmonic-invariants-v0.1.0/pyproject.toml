[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "seven-governors-harmonic-invariants"
version = "0.1.0"
description = "Exact Court geometry and Carey invariant registry."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
