# Release Notes

## 0.1.1 — 2026-07-30

Compatibility and epistemic-boundary release.

Changed:

- added `RegistryRelease`, source fingerprints, and explicit active-profile /
  active-semantic-operator relationships for side-by-side release loading;
- added file, snapshot, and Neo4j compiler providers;
- made `C_S` an ordered, non-metric semantic coordinate and retained its
  normalized value only as a display convention;
- separated hard requirements, soft priors, selectable reference pools, and
  creative affordances in creation packets;
- reclassified all four fixtures as structural/normalization evidence with
  `semanticEffectEvidence=false`;
- quarantined unresolved legacy references from `governors.yaml`;
- added a non-destructive v0.1.0 migration and active-release-scoped Cypher
  validation.

Preserved:

- all frozen source inputs;
- all seven canonical office assignments;
- all fifteen structural operator bindings;
- route-independent normal-form identity;
- unresolved aggregate `C_H` and unresolved operator-specific semantic deltas.

## 0.1.0 — 2026-07-30

Initial executable release.

Added:

- seven typed canonical Governor profiles;
- twenty-eight typed feature definitions;
- seven photonic records with reproducible `C_P`, frequency, and energy;
- an explicit unresolved `C_H` contract;
- authored `C_S` office/process ordinals;
- fifteen semantic operator shells bound to the mutation algebra;
- landforms as the first domain projection;
- deterministic packet compiler;
- Acoustic, Harmonic Minor, Lydian Minor, and Aeolian covariance fixtures;
- Neo4j schema, idempotent import, validations, and example queries;
- four JSON Schemas and executable QA.

Intentionally deferred:

- operator-specific semantic transforms;
- admitted promote/suppress/preserve/prohibit effects;
- a canonical aggregate harmonic-compression formula;
- domain projections beyond landforms;
- a semantic observation ledger populated from generated assets.
