[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "seven-governors-court-filter-algebra"
version = "0.1.0"
description = "Court filter algebra and mutation commutation evidence."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
